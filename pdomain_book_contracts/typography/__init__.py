"""
Typography contracts: labels, spans, records, and exchange formats.

Will own ``labels.py``, ``spans.py``, ``records.py``, ``annotations.py``,
``exchange.py``, ``book_manifest.py``, and ``review.py``, moved from
``pdomain_book_tools.typography``. Depends only on ``geometry``, ``text``,
and ``_schemas``.

Empty pending the code move in a later task.
"""

from __future__ import annotations
