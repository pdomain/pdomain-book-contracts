"""
Alignment and ground-truth matching between two texts.

Will own ``alignment.py``, ``engine.py``, ``models.py``,
``legacy_projection.py``, and ``pgdp_continuations.py``, moved from
``pdomain_book_tools.typography`` and ``pdomain_book_tools.matching``, plus
the matching vocabulary ``match_type.py`` and ``character_groups.py``, moved
from ``pdomain_book_tools.ocr.ground_truth_matching_helpers``. Depends on
``geometry``, ``text``, and ``typography``.

Empty pending the code move in a later task.
"""

from __future__ import annotations
